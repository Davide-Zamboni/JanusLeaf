# Local Development Profile
# Uses Docker PostgreSQL container (docker-compose.dev.yml)
# Run with: ./gradlew bootRun --args='--spring.profiles.active=local'

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/janusleaf
    username: janusleaf
    password: janusleaf

# Local logging - more verbose for debugging
logging:
  level:
    com.janusleaf: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
